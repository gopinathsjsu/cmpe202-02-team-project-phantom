# Mock Frontend Environment Configuration
# Copy this file to .env.local and update with your actual values

# ============================================
# Orchestrator API Configuration
# ============================================
# Base URL for the orchestrator service (used for authentication)
# Default port: 8080 (as per orchestrator/cmd/main.go)
NEXT_PUBLIC_ORCHESTRATOR_URL=http://localhost:8080

# ============================================
# Events Server WebSocket Configuration
# ============================================
# WebSocket URL for the events-server
# Format: ws://host:port/ws
# Check events-server PORT environment variable for the actual port
# Default port: 8001 (as per events-server)
NEXT_PUBLIC_EVENTS_SERVER_URL=ws://localhost:8001/ws

# ============================================
# WebSocket Heartbeat Configuration
# ============================================
# Presence heartbeat interval in seconds
# Should match WS_HEARTBEAT_SECONDS in events-server (.env file)
# Must be less than WS_DEAD_SECONDS to keep connection alive
# Default: 5 seconds (matches events-server default)
NEXT_PUBLIC_WS_HEARTBEAT_INTERVAL=5

# ============================================
# Notes on Other Services
# ============================================
# The following services are used by the backend but not directly by mock-frontend:
#
# Events-Server requires:
#   - PORT (WebSocket server port)
#   - ORCH_BASE_URL (orchestrator URL for auth verification)
#   - REDIS_ADDR, REDIS_PASSWORD, REDIS_DB
#   - WS_HEARTBEAT_SECONDS, WS_DEAD_SECONDS, PRESENCE_TTL_SECONDS
#   - RABBITMQ_URL, RABBITMQ_QUEUE_NAME
#
# Chat-Consumer requires:
#   - RABBITMQ_URL, RABBITMQ_QUEUE_NAME
#   - REDIS_ADDR, REDIS_PASSWORD, REDIS_DB
#   - MONGO_URI
#
# Orchestrator requires:
#   - PORT (defaults to 8080)
#   - DATABASE_URL
#   - CHAT_MONGO_URI (optional)
#   - RABBITMQ_URL, RABBITMQ_QUEUE_NAME (optional)
#   - LISTING_SERVICE_URL, LISTING_SERVICE_SHARED_SECRET (optional)

